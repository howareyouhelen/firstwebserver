<!DOCTYPE html>
<html lang="en">

<head>
    <title>TinyApp</title>
</head>

<body>
    <style>
        body {
            background-color: #46dac7;
        }
    </style>
    <header>
        <% include ./partials/_header %>
    </header>
    <ul>
        <% for (var id in urls) { %>
            <!--   for every shortid in the urls(obj) pass through shortid -->
            <li>
                <%= urls[id] %>
                    <li>
                        <a href=/u/<%=id%>><%= id%></a>
                    </li><br>
                    <a href="/urls/<%=id%>">Edit</a>
                    <form method="POST" action="/urls/<%= id%>/delete">
                        <input type="submit" value="Delete">
                    </form>
            </li>
    </ul>
    <% } %>
</body>

</html>